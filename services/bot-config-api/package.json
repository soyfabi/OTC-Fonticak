{
  "name": "fonticak-bot-config-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Cloudflare Worker for sharing Fonticak bot and options configs by hash",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "kv:create": "wrangler kv namespace create CONFIGS",
    "check": "node --check src/index.js",
    "test": "node --test test/index.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
